<?php

namespace App\Filament\Resources\DynamicPromptResource\Pages;

use App\Filament\Resources\DynamicPromptResource;
use Filament\Pages\Actions;
use Filament\Resources\Pages\ListRecords;
use Illuminate\Support\Facades\Cache;

class ListDynamicPrompts extends ListRecords
{
    protected static string $resource = DynamicPromptResource::class;
    public function mount(): void
    {

        Cache::set('folder_id',request()->folder_id);
        parent::mount(); // TODO: Change the autogenerated stub
    }
    protected function getActions(): array
    {

        return [
            Actions\CreateAction::make()->url(route('filament.resources.dynamic-prompts.create')."?folder_id=".Cache::get('folder_id')),
            Actions\Action::make("Back")->label('Back')->url(route('filament.resources.folders.view',Cache::get('folder_id'))),
        ];
    }
}
